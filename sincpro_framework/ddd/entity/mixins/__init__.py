"""Each mixin an `Entity` opts into, one file each: `AuditedMixin`, `ArchivableMixin`,
`ChangeTrackingMixin`. Re-exported by `sincpro_framework.ddd.entity`, so nothing outside this
package needs to know they live here rather than beside `Entity` itself.
"""
