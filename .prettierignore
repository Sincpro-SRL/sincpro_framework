# Generated by openwiki — never reformat it. The agent owns these files and
# rewrites them on every `make docs`; formatting them only creates noise diffs.
openwiki/
AGENTS.md
CLAUDE.md
.github/workflows/openwiki-update.yml

# Not ours / not source
.venv/
node_modules/
dist/
htmlcov/
site/
