# Files

- [MCP entrypoint: publishing the bus as FastMCP tools](entrypoint-mcp.md) - Owns the code-level path from a UseFramework instance to a runnable MCP server: the Entrypoint facade and build_mcp_server, the optional fastmcp extra and the ImportError it raises when missing, fastmcp_callable's keyword-only signature stamping (including default_factory as Annotated metadata), and the stdio versus Streamable HTTP choice.
- [JSON-RPC entrypoint: gateway, method names and OpenRPC discovery](entrypoint-rpc.md) - The RpcGateway host of the bus catalog: alias-keyed composition roots, the {alias}.{layer}.{DtoName} method namespace, by-name params, the jrpc error envelope, notification and batch semantics, context folded from request members and HTTP headers, rpc.discover plus the OpenRPC 1.4 document, and the Starlette/uvicorn app behind the optional rpc extra.
- [The shared entrypoint layer: catalog projection, Scalar execution and the JSON-safety decision](entrypoints-catalog.md) - The transport-agnostic boundary every driving adapter shares: introspection metadata projected into PackedFeatureOrAppService, Catalog with include/exclude/wrap and get_scalar_use_cases(filter_binaries_schema), scalar_executor's dict-in/dict-out execution with its trace/context split, and json_utils' full schema-tree walk that decides whether a DTO is publishable on a JSON wire.
- [Observability: error reporting to Sentry/GlitchTip](observability-errors.md) - The error half of observability in sincpro-framework: the isolated sentry_sdk.Client per release (never sentry_sdk.init), the DSN and tenant gates, the release rule with the bus excluded and told apart by sincpro.instance, record_error's tags and skip conditions, the reporting-before-handler ordering, ignore_sentry_exceptions, and the sentry status probe values.
- [Observability: tracing, log correlation and the process/provider model](observability-tracing.md) - The tracing half of sincpro-framework observability: the per-bus Observability door versus the process door, artifact:version:bus service.name, the per-bus TracerProvider that never takes the global one, install_process_provider and the PROCESS registry key, one span per DTO execution, FrameworkSpanContext's four resolution paths with their UUID fallback, and the status probe values that verify configuration without a backend.
