# Files

- [Building a bounded context: create the instance first, import the services second](building-a-bounded-context.md) - End-to-end walkthrough of standing up one bounded context — dependencies.py with DependencyContextType, framework.py with local Feature/ApplicationService bases, the __init__.py that creates the UseFramework instance before importing the service modules, and how to expose and invoke the result — plus the ordering traps (module never imported, module importing the wrong instance, build raised before the facade existed) and the failure each one produces.
- [Error handling: three scopes, one chain per bus, and reporting before the handler](error-handling.md) - The error-handler contract in sincpro-framework: ErrorHandler as (error) -> Any where returning a value suppresses and re-raising delegates, the three independent scopes (global on the facade, feature, app service) and exactly which exception each one sees, build_error_handler_chain/compose_handler first-registered-first-executed semantics, registering handlers before or after the build, and the per-layer observability.record_error that always runs before a handler can swallow the error.
