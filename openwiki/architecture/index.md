# Files

- [Executing a DTO: the end-to-end bus flow](bus-execution.md) - Traces one framework(dto) call through the lazy build, the implicit context overlay, the middleware pipeline and FrameworkBus routing into FeatureBus or ApplicationServiceBus, with every failure path and its handler semantics.
- [Concurrency: thread handoff, async facade and free-threading status](concurrency-and-context-handoff.md) - The two sanctioned ways to cross a thread boundary with context intact — Bus.thread_context()/ThreadContextBus, one captured contextvars snapshot per submitted task with a guided RuntimeError on concurrent reuse, and Bus.get_async_bus()/AsyncBus, stateless and reusable because asyncio.to_thread snapshots per call — plus why a bare executor.submit loses self.context, the global_scope exception, the ABC/TYPE_CHECKING wiring and stub contracts behind both handles, and the repository's current Python 3.14 and free-threading notes.
- [Architecture overview: buses, bounded contexts and module map](overview.md) - The structural map of sincpro-framework: one UseFramework instance as one bounded context, the three-level bus layering with its registry ownership, everything that attaches to an instance, and the on-disk module map grouped by owned system.
- [Registration and the IoC container: from decorated class to executable handler](registration-and-ioc.md) - How @framework.feature(...) / @framework.app_service(...) accumulate the FrameworkContainer registries at import time, what build_root_bus() materialises from them, the Singleton/Factory scoping per instance, why register_feature/register_app_service is a different entry point, and every duplicate, cross-layer and unknown-DTO failure the code actually raises.
