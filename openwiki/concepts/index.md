# Files

- [Configuration and settings: SincproConfig, $ENV: and the framework defaults](configuration.md) - How sincpro-framework reads configuration: the SincproConfig base model with its $ENV: resolution validator and type-checked fallback to field defaults, build_config_obj with sub_key, the shipped conf file and SINCPRO_FRAMEWORK_CONFIG_FILE, and what each DefaultFrameworkConfig field drives in the logger and in observability.
- [Context propagation: overlay, global_scope and self.context](context-propagation.md) - The framework's context store: ContextMixin's per-instance shared dict plus a per-task ContextVar overlay, FrameworkContext as the isolated with-block that merges and restores, global_scope=True publishing onto the instance and every live overlay, the implicit overlay UseFramework.__call__ pushes and pops, ContextConsumer.bind_to_framework as the reason self.context is the live dict, and what all of that means for trace ids, logs and error reporting.
- [Dependency injection and typing: add_dependency, deps and DependencyContextType](dependency-injection-and-typing.md) - Both halves of injection in sincpro-framework: the runtime path from add_dependency through dynamic_dep_registry and the build-time push that makes a dependency reachable as self.<name>, the read-only DependencyLocator behind framework.deps with its DependencyNotRegistered miss, and the typing path — UseFramework[DependencyContextType], the hand-written stubs shipped with py.typed, and the 'declare a typing-only class, mix it into local Feature bases' convention.
- [DTOs and Value Objects: pydantic contracts and validation](dto-and-value-objects.md) - Owns the data contract of sincpro-framework: DataTransferObject as a pydantic BaseModel with arbitrary_types_allowed and use_attribute_docstrings, the class-name indexing that registers and dispatches every DTO, and ValueObject(...) as a factory that builds a validated primitive subtype whose pydantic core/json schema hooks run validate_fn at construction and again at field hydration.
- [Feature versus ApplicationService: choosing the atomic or orchestration layer](feature-vs-application-service.md) - The one decision the two public handler abstractions encode — Feature as an atomic use case and ApplicationService as an orchestration that also receives the container's FeatureBus — plus what happens when the same DTO name lands in both layers, and what the shipped .pyi stubs do and do not give you.
- [Introspection: reading a built bus](introspection.md) - The one module that knows the bus's internal shape: built_bus() and its NOT_BUILT ValueError, features()/app_services()/dtos() returning described metadata, the four-step description resolution that reads only own docstrings, and why a DTO name missing from dto_registry makes a handler invisible.
- [Middleware pipeline: DTO processing before execution](middleware.md) - The pre-execution middleware chain of UseFramework: the Middleware callable protocol, add_middleware ordering, MiddlewarePipeline.execute running the chain then the bus executor, the __class__ restore that keeps a transformed DTO routable by its original registry key, and why a middleware exception never reaches the layer error handlers.
